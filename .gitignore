# Secrets
.env
# Local .env backups (e.g. nano's .env.save, editor copies) — never the example
.env.save
.env.save.*
.env.bak
.env.local

# Backups (may contain passwords)
backups/

# Docker volumes (if bind mounts are used)
data/

# Operating system
.DS_Store
Thumbs.db
